# 🧠 Правила для Claude AI - ML Crypto Trading System с Thinking LSP + Context7

## 🔴 КРИТИЧЕСКИ ВАЖНО - ВСЕГДА ИСПОЛЬЗОВАТЬ ВСЕ ТРИ КОМПОНЕНТА

### ⚡ АВТОМАТИЧЕСКАЯ АКТИВАЦИЯ:
```bash
# LSP сервер запускается автоматически при первом обращении
# Если нужно запустить вручную:
cd lsp_server && ./start_lsp_auto.sh
```

### 🎯 ДЛЯ КАЖДОГО ЗАПРОСА ОБЯЗАТЕЛЬНО:
1. **LSP КОНТЕКСТ** - Автоматически получать локальный контекст файлов
2. **CONTEXT7** - ВСЕГДА добавлять "use context7" в анализ 
3. **SEQUENTIAL THINKING** - Применять 5-шаговый анализ для важных файлов
4. **ИСТОРИЯ ИЗМЕНЕНИЙ** - Проверять что менялось ранее
5. **СИНТЕЗ** - Объединять все источники для лучшего решения

### ⚠️ БЕЗ ИСКЛЮЧЕНИЙ:
- Даже для простых вопросов - использовать LSP контекст
- Для любой работы с кодом - добавлять "use context7"
- Для критических файлов - полный Sequential Thinking анализ

### 🔥 ПРИМЕР ПРАВИЛЬНОГО МЫШЛЕНИЯ CLAUDE:
```
Запрос: "Оптимизируй загрузку данных"

Мышление Claude:
1. [LSP] Получаю контекст data/data_loader.py
2. [Context7] Запрашиваю актуальную документацию PostgreSQL/SQLAlchemy
3. [Thinking] Анализирую зависимости и риски
4. [История] Проверяю предыдущие изменения
5. [Синтез] Предлагаю оптимальное решение
```

### 📝 ШАБЛОН ДЛЯ КАЖДОГО ОТВЕТА:
```
[LSP контекст]: {файлы и зависимости}
[Context7]: {актуальная документация} 
[Sequential Thinking]: {анализ рисков}
[Решение]: {оптимальный код}
```

### ПРОЦЕСС МЫШЛЕНИЯ (обязательно для важных файлов):
```python
# Перед работой с файлом:
from lsp_server.thinking_lsp_integration import analyze_with_thinking
analysis = await analyze_with_thinking(file_path)

# Или через MCP Bridge:
from lsp_server.mcp_lsp_bridge import get_bridge
bridge = get_bridge()
context = bridge.get_file_context(file_path)
```

## 🌐 Язык общения
- **ВСЕГДА** отвечать на русском языке
- Комментарии в коде писать на русском
- Документацию писать на русском
- Логи и сообщения в коде на русском

## 📁 Работа с файлами
- **НЕ СОЗДАВАТЬ** новые файлы без явной просьбы
- **РЕДАКТИРОВАТЬ** только существующие файлы
- **НЕ СОЗДАВАТЬ** файлы с префиксами fix_, fixed_, new_, temp_
- При необходимости изменений - вносить их в текущий файл

## 🚫 Запреты
- НЕ создавать дубликаты файлов!!
- НЕ создавать временные файлы!!
- НЕ создавать файлы для исправлений - править оригинал!!
- НЕ удалять файлы без явного запроса!!!

## ✅ Рекомендации
- Перед созданием файла спрашивать разрешение
- Показывать, какие изменения будут внесены
- Использовать Edit tool для изменения существующих файлов
- Группировать связанные изменения в одном файле

## 🎯 Приоритеты проекта
- Использовать данные с ФЬЮЧЕРСНОГО рынка (не спот)
- Исключить TESTUSDT и другие тестовые символы
- Фокус на ML модели для криптотрейдинга
- PostgreSQL база данных на порту 5555
- Expected returns как основная метрика для обучения
- Частичные закрытия позиций (20%, 30%, 30% на разных уровнях)
- Защита прибыли (breakeven, profit locking)

## 🧠 LSP + CONTEXT7 ИНТЕГРАЦИЯ

### 🔥 ВСЕГДА ИСПОЛЬЗОВАТЬ КОМБИНАЦИЮ:
1. **LSP** - для локального контекста проекта
2. **Context7** - для актуальной документации библиотек
3. **Sequential Thinking** - для глубокого анализа

### Примеры использования:
```
# При работе с PyTorch:
"Оптимизируй модель PatchTST для GPU. use context7"
+ LSP контекст из models/patchtst.py

# При работе с PostgreSQL:
"Оптимизируй запросы к БД. use context7"
+ LSP контекст из data/data_loader.py

# При добавлении новых библиотек:
"Интегрируй библиотеку XGBoost. use context7"
+ LSP анализ зависимостей
```

### Автоматические проверки перед операциями:
```python
# 1. Получить контекст файла
context = mcp_get_file_context(file_path)

# 2. Проверить связанные файлы
related = context['related_files']

# 3. Проверить последние изменения
changes = mcp_get_recent_changes(5)

# 4. Для важных файлов - глубокий анализ
if is_important_file(file_path):
    analysis = analyze_with_thinking(file_path)
    risk_level = analysis['final_analysis']['risk_level']
    recommendations = analysis['final_analysis']['recommendations']
```

### 📊 УРОВНИ ВАЖНОСТИ ФАЙЛОВ И ГЛУБИНА АНАЛИЗА:

#### 🚨 КРИТИЧЕСКИЕ (5-шаговый анализ + подтверждение):
- `models/patchtst.py`, `models/patchtst_improved.py` - основная архитектура
- `config/config.yaml` - влияет на ВСЮ систему
- `data/data_loader.py`, `data/feature_engineering.py` - подготовка данных
- `training/trainer.py`, `training/optimizer.py` - процесс обучения
- `trading/risk_manager.py` - управление рисками
- `models/ensemble.py` - ансамблирование моделей

#### ⚡ ВАЖНЫЕ (3-шаговый анализ):
- `main.py`, `train_model.py` - точки входа
- `trading/signals.py`, `trading/position_sizer.py` - торговая логика
- `training/validator.py` - валидация моделей
- `data/bybit_loader.py` - загрузка рыночных данных

#### 📋 СТАНДАРТНЫЕ (базовый анализ):
- `utils/*` - вспомогательные функции
- `monitor_training.py` - мониторинг процесса
- `notebooks/*.ipynb` - анализ и визуализация

#### 📌 НИЗКИЙ ПРИОРИТЕТ (минимальный анализ):
- `lsp_server/*` - IDE интеграция
- `scripts/*`, `tests/*` - вспомогательные скрипты
- `*.md`, `*.sh` - документация и скрипты

### База данных изменений:
- Путь: `lsp_server/.lsp_data/file_tracking.db`
- Таблицы: file_states, file_changes, context_requests
- Автоматическое отслеживание всех модификаций

## 📊 Структура проекта ML Crypto Trading

### Основные компоненты:
```
crypto_ai_trading/
├── config/               # Конфигурация системы
│   └── config.yaml      # ⚠️ КРИТИЧЕСКИЙ ФАЙЛ - влияет на все
├── data/                 # Загрузка и обработка данных
│   ├── data_loader.py   # PostgreSQL интеграция
│   └── feature_engineering.py # 100+ индикаторов
├── models/               # ML модели
│   ├── patchtst.py      # ⭐ ГЛАВНАЯ АРХИТЕКТУРА
│   └── ensemble.py      # Ансамблирование
├── trading/              # Торговые стратегии
│   ├── signals.py       # Генерация сигналов
│   ├── risk_management.py # Управление рисками
│   └── position_manager.py # 6 стратегий позиций
├── training/             # Обучение моделей
│   ├── trainer.py       # Основной процесс обучения
│   └── validator.py     # Валидация моделей
├── utils/                # Утилиты
│   ├── metrics.py       # Метрики производительности
│   ├── visualization.py # Графики и отчеты
│   └── logger.py        # Логирование
├── lsp_server/          # LSP + Thinking интеграция
│   ├── enhanced_lsp_server.py # LSP сервер
│   ├── mcp_lsp_bridge.py     # MCP мост
│   └── thinking_lsp_integration.py # Sequential Thinking
└── main.py              # Главная точка входа
```

### Ключевые особенности:
1. **PatchTST архитектура** - передовая модель для временных рядов
2. **100+ технических индикаторов** - полный набор для анализа
3. **Многозадачное обучение** - цена + вероятности TP/SL
4. **6 стратегий управления позициями** - Kelly, Volatility-based и др.
5. **PostgreSQL на порту 5555** - надежное хранение данных
6. **LSP с Thinking** - умный анализ перед изменениями

## 🔧 Команды и процессы

### Основные команды:
```bash
# Демо режим
python main.py --mode demo

# Полное обучение
python main.py --mode full

# Только подготовка данных
python main.py --mode data

# Только обучение (если данные готовы)
python main.py --mode train

# Бэктестинг
python main.py --mode backtest
```

### LSP команды:
```bash
# Статус LSP сервера
cd lsp_server && ./venv/bin/python start_lsp_service.py status

# Тест thinking анализа
cd lsp_server && ./venv/bin/python thinking_lsp_integration.py

# Просмотр истории изменений
cd lsp_server && ./venv/bin/python mcp_lsp_bridge.py
```

## 📈 Мониторинг и визуализация

### TensorBoard:
```bash
tensorboard --logdir logs/
```

### Встроенный мониторинг:
```bash
python monitor_training.py
```

### LSP логи:
- `lsp_server/lsp_service.log` - основной лог
- `lsp_server/enhanced-lsp.log` - детальный лог LSP
- `lsp_server/.lsp_data/` - база данных изменений

## 🚨 Критические правила для Claude

### ПЕРЕД редактированием config.yaml:
1. Проанализировать через Thinking LSP
2. Проверить все зависимые модули
3. Убедиться в корректности значений
4. Предупредить о влиянии на систему

### ПЕРЕД изменением models/patchtst.py:
1. Глубокий анализ архитектуры
2. Проверка совместимости с trainer.py
3. Валидация размерностей и типов
4. Тестирование на малом датасете

### ПЕРЕД работой с trading/:
1. Понимание текущих стратегий
2. Проверка risk_management.py
3. Анализ влияния на позиции
4. Бэктестинг изменений

## 💡 Рекомендации по разработке

### Всегда использовать LSP контекст:
```python
# Плохо:
with open('config/config.yaml', 'r') as f:
    config = yaml.load(f)

# Хорошо:
context = mcp_get_file_context('config/config.yaml')
# Проанализировать context['usage_hints']
# Проверить context['related_files']
with open('config/config.yaml', 'r') as f:
    config = yaml.load(f)
```

### Думать перед действием:
1. Получить контекст
2. Проанализировать зависимости
3. Оценить риски
4. Следовать рекомендациям
5. Выполнить изменения

## 📊 Текущий статус проекта

### ✅ Что работает:
- LSP сервер активен и индексирует проект
- Sequential Thinking анализирует важные файлы
- История изменений отслеживается автоматически
- MCP Bridge предоставляет контекст

### 🔄 В процессе:
- Обучение модели на полном датасете
- Оптимизация гиперпараметров
- Тестирование стратегий

### 📝 TODO:
- Расширить набор признаков
- Добавить новые источники данных
- Улучшить визуализацию результатов

## 🎯 ГЛАВНОЕ ПРАВИЛО - ТРОЙНАЯ ИНТЕГРАЦИЯ

### ⚡ КАЖДЫЙ ЗАПРОС = LSP + CONTEXT7 + THINKING

**АВТОМАТИЧЕСКИЙ ПРОЦЕСС CLAUDE**:
```
1. [LSP] → Получить локальный контекст файлов
2. [Context7] → Загрузить актуальную документацию (use context7)
3. [Thinking] → Проанализировать риски и зависимости
4. [История] → Проверить предыдущие изменения
5. [Решение] → Синтезировать оптимальный код
```

### 🔴 ОБЯЗАТЕЛЬНО ДЛЯ ВСЕХ ОПЕРАЦИЙ:
- **Чтение файла** → LSP контекст + история изменений
- **Написание кода** → Context7 документация + LSP анализ
- **Модификация** → Sequential Thinking + проверка рисков
- **Даже простой вопрос** → Использовать все три компонента

### 📋 Чек-лист перед кодированием:
- [ ] LSP контекст получен? 
- [ ] Context7 добавлен в промпт?
- [ ] Sequential Thinking выполнен для важных файлов?
- [ ] История изменений проверена?
- [ ] Риски оценены?

### 💡 Пример правильного запроса:
```
"Оптимизируй загрузку данных в data_loader.py для работы с большими датасетами. use context7"
```
Claude автоматически:
1. Получит LSP контекст файла
2. Загрузит актуальную документацию PostgreSQL/SQLAlchemy
3. Проанализирует зависимости и риски
4. Предложит оптимальное решение

---

*Этот файл автоматически загружается Claude Code и определяет правила работы с проектом crypto_ai_trading с использованием LSP + Context7*